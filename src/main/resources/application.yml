spring:
  data:
    mongodb:
      host: ${MONGO_DB_HOSTNAME:localhost}
      port: 27017
      database: local

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
